# ============================================================================
# PRODUCTION ENVIRONMENT VARIABLES
# Managed by Vin's human assistant
# NO MOCKING - Production-grade configuration
# ============================================================================

# ============================================================================
# REQUIRED - Core Infrastructure
# ============================================================================

# Weaviate Vector Database (REQUIRED)
# Purpose: Store embeddings and enable fast similarity search
# Used by: backend/database/vector_db_client.py, backend/database/knowledge_graph.py
# How to get: Runs automatically in Docker Compose (no auth needed for local)
WEAVIATE_URL=http://localhost:8080

# Grok API (REQUIRED)
# Purpose: Phone screen conversations, interview prep generation, entity extraction
# Used by: backend/integrations/grok_api.py, backend/interviews/phone_screen_interviewer.py, backend/matching/interview_prep_generator.py
# How to get: Sign up at https://docs.x.ai/docs/tutorial, get API key from X AI dashboard
GROK_API_KEY=your_grok_api_key_here

# ============================================================================
# REQUIRED - Outbound Data Gathering (Ishaan's Work)
# ============================================================================

# GitHub API (REQUIRED for outbound gathering)
# Purpose: Gather candidate information from GitHub profiles and repositories
# Used by: backend/integrations/github_api.py, backend/orchestration/outbound_gatherer.py
# How to get: Go to https://github.com/settings/tokens, generate personal access token with 'public_repo' scope
GITHUB_TOKEN=your_github_token_here

# X API (REQUIRED for outbound gathering)
# Purpose: Gather candidate information from X (Twitter) profiles and posts
# Used by: backend/integrations/x_api.py, backend/orchestration/outbound_gatherer.py
# How to get: Apply for X API access at https://developer.twitter.com/
# Note: X API v2 requires multiple credentials
X_API_KEY=your_x_api_key_here
X_API_SECRET=your_x_api_secret_here
X_BEARER_TOKEN=your_x_bearer_token_here

# LinkedIn API (OPTIONAL - May be restricted)
# Purpose: Gather candidate information from LinkedIn profiles
# Used by: backend/integrations/linkedin_api.py, backend/orchestration/outbound_gatherer.py
# How to get: LinkedIn API access is restricted - may need to use alternative methods
# Note: If unavailable, system will skip LinkedIn gathering
LINKEDIN_API_KEY=your_linkedin_api_key_here
LINKEDIN_API_SECRET=your_linkedin_api_secret_here

# ============================================================================
# OPTIONAL - Configuration
# ============================================================================

# Logging Level (OPTIONAL)
# Purpose: Control application logging verbosity
# Used by: backend/__init__.py
# Valid values: DEBUG, INFO (default), WARNING, ERROR
LOG_LEVEL=INFO

# ============================================================================
# NOTES
# ============================================================================

# arXiv API: No credentials needed - public API
# - Used by: backend/integrations/arxiv_api.py
# - Endpoint: http://export.arxiv.org/api/query (public, no auth)

# ============================================================================
# SETUP INSTRUCTIONS FOR VIN'S HUMAN ASSISTANT
# ============================================================================

# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in all REQUIRED variables (marked above)
# 3. Weaviate: No setup needed - runs in Docker Compose automatically
# 4. Grok API: Sign up at https://docs.x.ai/docs/tutorial
# 5. GitHub: Generate token at https://github.com/settings/tokens (public_repo scope)
# 6. X API: Apply at https://developer.twitter.com/ (may take time for approval)
# 7. LinkedIn: May be restricted - check availability
# 8. Verify .env is in .gitignore (should already be there)

# ============================================================================
# VALIDATION
# ============================================================================

# To verify all required variables are set:
# python3 -c "
# import os
# from dotenv import load_dotenv
# load_dotenv()
# required = ['WEAVIATE_URL', 'GROK_API_KEY', 'GITHUB_TOKEN', 'X_API_KEY', 'X_API_SECRET', 'X_BEARER_TOKEN']
# missing = [v for v in required if not os.getenv(v)]
# if missing:
#     print(f'❌ Missing: {missing}')
# else:
#     print('✅ All required variables set')
# "
